import React from 'react';

const Quizees  = ({quize}) => {
    // console.log(quize)
    const {options,question,correctAnswer} = quize;
    return (
        <div>
            
        </div>
    );
};

export default Quizees ;